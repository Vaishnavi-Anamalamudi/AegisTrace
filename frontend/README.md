# Frontend

The current frontend is server-rendered with Thymeleaf and lives under:

```text
src/main/resources/templates
src/main/resources/static
```

This folder documents frontend ownership for contributors without moving source files away from Maven/Spring Boot conventions.

Frontend changes should preserve the existing dark SOC interface unless an approved issue explicitly targets UI redesign.
